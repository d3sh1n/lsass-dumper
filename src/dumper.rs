//! Legacy dumper module — kept as stub.
//! All dump functionality moved to minidump.rs + handle.rs

// This module is retained for backward compatibility but is no longer used.
// The hand-crafted minidump in minidump.rs replaces MiniDumpWriteDump entirely.
